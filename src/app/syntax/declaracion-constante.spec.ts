import { DeclaracionConstante } from './declaracion-constante';

describe('DeclaracionConstante', () => {
  it('should create an instance', () => {
    expect(new DeclaracionConstante()).toBeTruthy();
  });
});

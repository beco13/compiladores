import { DeclaracionFuncion } from './declaracion-funcion';

describe('DeclaracionFuncion', () => {
  it('should create an instance', () => {
    expect(new DeclaracionFuncion()).toBeTruthy();
  });
});

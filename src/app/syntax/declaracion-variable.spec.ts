import { DeclaracionVariable } from './declaracion-variable';

describe('DeclaracionVariable', () => {
  it('should create an instance', () => {
    expect(new DeclaracionVariable()).toBeTruthy();
  });
});
